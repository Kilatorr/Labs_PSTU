#include <iostream>
using namespace std;

int main (){
    int a, b, c;
    cin >> a >> b >> c;
    if (a > b && a > c){
        cout<<"a bigger";
    }
    if (b > a && b > c){
        cout<<"b bigger";
    }
    if (c > b && c > b){
        cout<<"c bigger";
    }
;
;
    return 0;
}
