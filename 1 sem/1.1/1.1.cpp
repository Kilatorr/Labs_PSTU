#include <iostream>
using namespace std;

int main (){
    int a, b, c;
    cin >> a >> b >> c;
    if (a > b && a > c){
        cout<<"a skebed";
    }
    if (b > a && b > c){
        cout<<"b skebed";
    }
    if (c > b && c > b){
        cout<<"c skebed";
    }
;
;
    return 0;
}